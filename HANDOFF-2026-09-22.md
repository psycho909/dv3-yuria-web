# Yuria handoff — 2026-09-22

## Scope

This handoff covers the first shared GitHub repository for the A deterministic calculator and the B browser MVP. The repository remote is `https://github.com/psycho909/dv3-yuria-web.git` and the intended default branch is `main`.

## Git handoff state

- Published branch: `main`
- Latest published UI commit: `dc144cb` (`feat: redesign yuria calculator UI and flow`)
- Handoff update commit: `0fe91f6` (`docs: add git and deployment handoff`)
- Local branch tracking: `main -> origin/main`
- Handoff file: `HANDOFF-2026-09-22.md` in the repository root
- No secrets, `.env`, `node_modules`, `dist`, or Vercel local credentials are tracked.

## Delivered

- A keeps the 22-card catalog, executable special effects, color tiers, failure score, seeded Monte Carlo recommendation, exact final-turn distribution for integer red rolls, zero-hit expected-score fallback, Jev semantic objective routing, and the five recorded score audits.
- B contains a Vite page with current-run history, success/failure/removal state, three-candidate input, activation probability, target probability, expected score, P10/P50/P90, range, color counts, reward thresholds, card catalog, formula notes, and recorded/model statistics.
- B now starts with no selected cards (`0 / 5`) and three empty candidate slots. Recommendation calculation stays unavailable until three unique, non-conflicting candidates are selected, matching the in-game setup flow.
- B candidate slots now follow the reference UI: each slot has a popup card picker plus direct blue/purple/red color buttons, and the popup prevents duplicate cards before applying a choice.
- The current red default is integer-percent uniform. The 1648 observation is retained as evidence for a continuous-red or intermediate-rounding hypothesis; it is not silently used to alter the default.
- Jev is a semantic objective router only. Numeric probability, score, color, special-effect, and ranking math remains deterministic code.

## Validation evidence

Run from the project directory shown:

```text
A: npm install             PASS
A: npm test                PASS — 9 files, 62 tests
A: npm run typecheck       PASS
A: npm run demo            PASS — 3 ranked candidates with mean and threshold probability
B: npm install             PASS
B: npm test                PASS — 2 files, 14 tests
B: npm run typecheck       PASS
B: npm run build           PASS — Vite production bundle generated
```

The web smoke test rendered the initial empty-card setup (`0 / 5`, three blank candidate slots), opened the card picker from a slot, selected a card and purple color, applied the choice, and confirmed the slot state. It also confirmed direct color switching, duplicate-card disabling in the picker, three unique candidate selection, and the deterministic recommendation panel. The title, 22-card catalog label, 1500 target, recorded scores `748 / 972 / 1270 / 840 / 1648`, 30-game model statistics, reward ladder, and formula panels remain present.

The redesigned B UI acceptance suite passed 18/18 cases on Edge 153 at 375/768/1024/1440px and on the production alias, with zero page errors. Full evidence is recorded in `DESIGN.md` §17 and `B_yuria-web-spec/artifacts/acceptance/browser-results.json`.

## Vercel deployment

- Project: `dv3-yuria-web` (`psycho909s-projects`)
- Production alias: https://dv3-yuria-web.vercel.app
- Deployment: `dpl_HEdVT5Hz3LZnvPog6shM2MjLpxgB`
- Status: `READY`, target `production`
- Inspect: https://vercel.com/psycho909s-projects/dv3-yuria-web/HEdVT5Hz3LZnvPog6shM2MjLpxgB
- Deployment was built from the validated B directory with Vercel CLI 59.25.0 using `pull --environment=production`, `build --prod`, and `deploy --prebuilt --prod`. Git auto-deploy is not connected yet; future pushes require a new Vercel deploy unless the project is connected in Vercel.

## Known limits

- Future card-category distribution is still category-weighted with uniform cards inside a category; future colors are independent uniform draws and may repeat.
- Earlier future turns use a one-step expected-value rollout policy. The fifth turn uses the exact integer-red distribution.
- Star removal target weights and Sun self-count remain explicit assumptions.
- Yuria's blessing is intentionally disabled until its random digit rule is measured.
- The repository has two moderate development-only Vitest audit findings; `npm audit --omit=dev` reports zero production vulnerabilities. Upgrading Vitest to 5 is a separate breaking-change task.

## Next handoff action

```powershell
git clone https://github.com/psycho909/dv3-yuria-web.git
cd dv3-yuria-web
git fetch origin
git branch -vv
cd B_yuria-web-spec
npm install
npm run dev
```

For the next iteration, collect more real game records containing red counts, Star removal targets, Sun activation, and final scores before changing defaults. Keep every new rule behind a named configuration flag and add a seeded regression test.

## Suggested skills

- `handoff` for the next cross-session state snapshot.
- `vercel:agent-browser-verify` or the local browser QA workflow for interactive page checks.
- `matt-skills-curated:research` only when an external game rule or provider contract needs primary-source verification.
