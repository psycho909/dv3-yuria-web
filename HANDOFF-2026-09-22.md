# Yuria handoff — 2026-09-22

## Scope

This handoff covers the first shared GitHub repository for the A deterministic calculator and the B browser MVP. The repository remote is `https://github.com/psycho909/dv3-yuria-web.git` and the intended default branch is `main`.

## Git handoff state

- Published branch: `main`
- Latest published UI commit: `dc144cb` (`feat: redesign yuria calculator UI and flow`)
- Handoff update commit: `0fe91f6` (`docs: add git and deployment handoff`)
- Local branch tracking: `main -> origin/main`
- Handoff file: `HANDOFF-2026-09-22.md` in the repository root
- No secrets, `.env`, `node_modules`, `dist`, or Vercel local credentials are tracked.

## Delivered

- A keeps the 22-card catalog, executable special effects, color tiers, failure score, seeded Monte Carlo recommendation, exact final-turn distribution for integer red rolls, zero-hit expected-score fallback, Jev semantic objective routing, and the five recorded score audits.
- B contains a Vite page with current-run history, success/failure/removal state, three-candidate input, activation probability, target probability, expected score, P10/P50/P90, range, color counts, reward thresholds, card catalog, formula notes, and recorded/model statistics.
- B now starts with no selected cards (`0 / 5`) and three empty candidate slots. Recommendation calculation stays unavailable until three unique, non-conflicting candidates are selected, matching the in-game setup flow.
- B candidate slots now follow the reference UI: each slot has a popup card picker plus direct blue/purple/red color buttons, and the popup prevents duplicate cards before applying a choice.
- The current red default is integer-percent uniform. The 1648 observation is retained as evidence for a continuous-red or intermediate-rounding hypothesis; it is not silently used to alter the default.
- Jev is a semantic objective router only. Numeric probability, score, color, special-effect, and ranking math remains deterministic code.

## Validation evidence

Run from the project directory shown:

```text
A: npm install             PASS
A: npm test                PASS — 9 files, 62 tests
A: npm run typecheck       PASS
A: npm run demo            PASS — 3 ranked candidates with mean and threshold probability
B: npm install             PASS
B: npm test                PASS — 2 files, 14 tests
B: npm run typecheck       PASS
B: npm run build           PASS — Vite production bundle generated
```

The web smoke test rendered the initial empty-card setup (`0 / 5`, three blank candidate slots), opened the card picker from a slot, selected a card and purple color, applied the choice, and confirmed the slot state. It also confirmed direct color switching, duplicate-card disabling in the picker, three unique candidate selection, and the deterministic recommendation panel. The title, 22-card catalog label, 1500 target, recorded scores `748 / 972 / 1270 / 840 / 1648`, 30-game model statistics, reward ladder, and formula panels remain present.

The redesigned B UI acceptance suite passed 18/18 cases on Edge 153 at 375/768/1024/1440px and on the production alias, with zero page errors. Full evidence is recorded in `DESIGN.md` §17 and `B_yuria-web-spec/artifacts/acceptance/browser-results.json`.

## Vercel deployment

- Project: `dv3-yuria-web` (`psycho909s-projects`)
- Production alias: https://dv3-yuria-web.vercel.app
- Deployment: `dpl_HEdVT5Hz3LZnvPog6shM2MjLpxgB`
- Status: `READY`, target `production`
- Inspect: https://vercel.com/psycho909s-projects/dv3-yuria-web/HEdVT5Hz3LZnvPog6shM2MjLpxgB
- Deployment was built from the validated B directory with Vercel CLI 59.25.0 using `pull --environment=production`, `build --prod`, and `deploy --prebuilt --prod`. Git auto-deploy is not connected yet; future pushes require a new Vercel deploy unless the project is connected in Vercel.

## Known limits

- Future card-category distribution is still category-weighted with uniform cards inside a category; future colors are independent uniform draws and may repeat.
- Earlier future turns use a one-step expected-value rollout policy. The fifth turn uses the exact integer-red distribution.
- Star removal target weights and Sun self-count remain explicit assumptions.
- Yuria's blessing is intentionally disabled until its random digit rule is measured.
- The repository has two moderate development-only Vitest audit findings; `npm audit --omit=dev` reports zero production vulnerabilities. Upgrading Vitest to 5 is a separate breaking-change task.

## Next handoff action

```powershell
git clone https://github.com/psycho909/dv3-yuria-web.git
cd dv3-yuria-web
git fetch origin
git branch -vv
cd B_yuria-web-spec
npm install
npm run dev
```

For the next iteration, collect more real game records containing red counts, Star removal targets, Sun activation, and final scores before changing defaults. Keep every new rule behind a named configuration flag and add a seeded regression test.

## Suggested skills

- `handoff` for the next cross-session state snapshot.
- `vercel:agent-browser-verify` or the local browser QA workflow for interactive page checks.
- `matt-skills-curated:research` only when an external game rule or provider contract needs primary-source verification.

## 2026-09-23 continuation

- GitHub `main` was fast-forwarded to `d09efe8` before this iteration. The deterministic B engine's Monte Carlo hot path was optimized without changing rules, seeds, draw count, Jev's role, or outputs. The final-turn exact-red branch now calculates the base score once per outcome, and repeated card-list allocation/sorting in future draws was removed.
- A fixed-seed distribution regression was added. The sub-agent compared 12 recommendations and 24 offer results against the pre-change HEAD with byte-for-byte JSON equality. Local `npm test` passed 15/15, `npm run typecheck` passed, `npm run build` passed, and `git diff --check` passed.
- Same-machine Core Ultra 7 165H/Node v22.14.0 turn-one timings improved from 1254–1413ms to 615–683ms for 3 candidates × 10,000 samples each; independent five-run check was 605–689ms. Five samples do not establish P95. Retest on the original i5-8400 and in the browser Worker before marking the desktop <800ms acceptance target complete.
- `B_yuria-web-spec/tests/keyboard-flow.cjs` passed against the production alias in Edge at 768px: keyboard-only picker, card selection, failure recording, and undo; history returned from one entry to zero with no page errors. A true browser 200% zoom test and actual screen-reader test remain pending. A CSS `zoom` experiment was not treated as browser-zoom evidence.
- Code commit `91dfbfa` was pushed to GitHub `origin/main`. Vercel deployment `dpl_fE6M8DgMzsTfcPLiTApGaJJB3pfY` is `READY`, target `production`; `https://dv3-yuria-web.vercel.app/` resolves to that deployment. Public HTML returned HTTP 200 and references `index-Bl5Uvdlj.js` and `index-29OOJ2wB.css`. The keyboard failure/undo script passed again on the new alias with no page errors. Git auto-deploy remains disconnected; future code pushes require a separate Vercel deployment.

## 2026-09-23 QA continuation

- The user waived 200% browser-zoom acceptance. This is a waiver, not a tested PASS. Actual screen-reader operation remains unverified; an Edge/Playwright ARIA snapshot showed a named main area and card picker dialog, labeled search, and grouped color controls.
- A real scoring bug was reproduced on production: changing an activated Star to failed left its prior target removed and the label stale. B now reverses the removal when Star becomes failed; reactivating or retargeting Star requires a chosen target. Tower failure clears its proc and reactivation requires a real proc choice. The Star-managed removal checkbox is disabled so corrections use the Star dialog.
- `tests/history-special-edit.cjs` passes on local preview and production, including Star failure, reactivation, retargeting, Tower correction, and cancel. Local `npm test` 15/15, typecheck, build, and `tests/browser-acceptance.cjs` 18/18 pass with zero page errors. The production keyboard failure/undo script passes again.
- Browser performance is a confirmed open issue: 30 sequential production Edge 153 Worker computations on a Core Ultra 7 165H, with 3 candidates and 10,000 samples per candidate, had 566ms minimum, 1039ms median, 1314ms P95, 1328ms maximum. This excludes the 150ms debounce and rendering; it does not meet the desktop P95 <800ms target. Reproduce with `tests/browser-worker-performance.cjs`. Do not replace this with the faster Node benchmark or reduce samples without an accuracy comparison.
- Code commit `175294a` was pushed to GitHub. Vercel deployment `dpl_57PMFmUAaLgZ9e8SKwuQ4MbT3Ybj` is `READY` / `production` and aliased to `https://dv3-yuria-web.vercel.app/`; public HTML returned HTTP 200 and loaded `index-Bm9x20Da.js`. Git auto-deploy remains disconnected.
- `REAL_GAME_DATA_PLAN.md` is a draft for local-first completed-game records and JSON export, not implemented. Do not merge simulated games or partial historical records into complete real-game calibration data.

## 2026-09-23 real-game records, cloud setup, and UI handoff

- Canonical clone remains `D:\Codex\dv3\_publish` on `main`; A was not changed in this iteration. B added `src/real-game-record.ts`, IndexedDB storage, JSON export/import, correction revisions, read-only statistics, and a fifth-round action that only archives after the player presses 「紀錄本局」. Actual game score is optional and separate from deterministic model score. The five old score-only observations and 30 simulations remain separate. `catalogProbability` is catalog input; `observedProbability` stays `null` until truly observed.
- Independent Supabase project `dv3-yuria` in organization `kvmbbhbefiwljyzubssu`, Tokyo `ap-northeast-1`, ref `kihgibfacsvbmvvmohuc` was created after user confirmation of the displayed US$0/month cost. Migration `B_yuria-web-spec/supabase/migrations/202609230001_real_games.sql` was applied. `public.real_games` has RLS and owner-only SELECT/INSERT/UPDATE policies; security advisor returned zero lints. Browser code includes only a publishable key. The user chose GitHub instead of Google login. GitHub OAuth provider still needs Client ID/Secret configured in the Supabase dashboard; neither actual login nor two-account isolation has been end-to-end tested. Do not claim cloud sync is usable until these pass.
- OAuth setup: at `https://github.com/settings/developers`, create an OAuth app with Homepage URL `https://dv3-yuria-web.vercel.app/` and callback `https://kihgibfacsvbmvvmohuc.supabase.co/auth/v1/callback`. Enter its Client ID/Secret only at `https://supabase.com/dashboard/project/kihgibfacsvbmvvmohuc/auth/providers` under GitHub. Set Site URL to the production URL and allow the same URL as an auth redirect; local `http://127.0.0.1:4174/` needs an extra redirect entry if local OAuth testing is desired. Never put the OAuth secret in Git or chat.
- Layout was changed in `src/usability.css`: full-viewport shell with 12–32px side padding; desktop left/right ratio 2:3 with selected cards in two columns and candidates in three. Card widths differ by ≤4px at 1440px. Selected cards have a consistent 184px face; reset control moved above them with a visible rust outline. Tablet candidates use two columns, mobile uses one. Visual evidence is in `B_yuria-web-spec/artifacts/real-game-flow/`.
- Local validation: `npm test` 18/18, `npm run typecheck`, `npm run build`, `npm audit --omit=dev` zero production findings. `tests/browser-acceptance.cjs` 18/18 and `tests/real-game-flow.cjs` passed with zero page errors on local preview. The real-game flow also passed against production. Production alias returned HTTP 200 with `index-Da7bAXVQ.js` and `index-C1PzMawK.css`. The browser Worker P95 and real screen-reader limits from DESIGN remain open; the user waived only 200% zoom.
- GitHub `main` contains commits `839e33e` (records/cloud preparation), `2b93383` (selected-card layout), and `d8f29c8` (full width, matched candidates, visible reset). Vercel deployment `dpl_8Vi49A7EpBnhcoahuFtRQEFdWVBg` is `READY`, target `production`, aliased to `https://dv3-yuria-web.vercel.app/`. `npx --yes vercel@59.25.0 pull --yes --environment=production`, `build --prod`, and `deploy --prebuilt --prod --yes` were used; Git auto-deploy remains disconnected.
- Next operator: finish GitHub OAuth provider configuration and test sign-in, upload after explicit record action, cross-device download, sign-out, and two-account RLS isolation. Then update this handoff with actual evidence. No change to deterministic scoring or Jev role is authorized by these records alone.

## 2026-09-23 GitHub OAuth verification

- GitHub OAuth app `尤里亞的占卜計算器` was created at `https://github.com/settings/applications/3876273`. Homepage is `https://dv3-yuria-web.vercel.app/`; callback is `https://kihgibfacsvbmvvmohuc.supabase.co/auth/v1/callback`. Wildcard matching and Device Flow are off. No client secret is stored in this repository.
- Supabase Auth Site URL and sole redirect URL are `https://dv3-yuria-web.vercel.app/`. GitHub provider showed Enabled after the user entered and saved the Client Secret directly in Supabase.
- On the production alias, GitHub authorization requested only read-only email address access. After the user's explicit confirmation, OAuth returned to the app and showed a signed-in account. Cloud download completed with `雲端 0 局；新增 0 局，略過相同 0 局。`. Sign-out showed the signed-out state, and re-login restored the signed-in state. The production page was left signed in for the user.
- This validates provider configuration, login callback, session display, empty cloud SELECT, sign-out, and re-login. It does not validate completed-game upload, non-empty cross-device download, or two-account RLS isolation. Do not mark those as PASS until real tests run. Deterministic recommendation math and Jev routing were not changed.
- This section supersedes the prior OAuth setup item under 2026-09-23 real-game records. Next operator should start with a real five-turn record and verify upload/download, then use a second account to test RLS isolation.

## 2026-09-23 recommendation documentation and color controls

- `B_yuria-web-spec/RECOMMENDATION-ALGORITHM.md` now describes the implemented 22-card catalog, score formula, special effects, color tiers, seeded simulation and exact final-turn branch, objectives, fallback, and unverified assumptions. Use it as the current-behavior baseline before future algorithm changes; the older `ALGORITHM-DATA-STRUCTURE.md` also contains roadmap concepts.
- `B_yuria-web-spec/src/pending.css` now shows unselected candidate/picker colors with white backgrounds and color-matched text/borders; selected colors use dark blue/purple/red backgrounds with white text. Existing `aria-pressed`, checkmarks, and focus styles remain. Sub-agent validation: `npm test` 18/18, typecheck, build, `git diff --check`; local 1280px interactive color/ARIA check passed. Mobile breakpoint CSS was inspected, not tested on a device.
- GitHub `main` commit `af3cb95` contains the document, README update, and CSS change. Vercel deployment `dpl_A74ecaak29nUVxGKwdy6QwqGwUs9` reported `READY`, target `production`, aliased to `https://dv3-yuria-web.vercel.app/`. The production browser showed unselected white buttons, a selected purple button with purple background/white text, and updated `aria-pressed` after clicking. No recommendation math changed.

## 2026-09-23 V2.1 and Git deployment handoff

- Current Git checkpoint before this handoff: `main` at `9816b79` (`Improve calculator card layout controls`), matching `origin/main`. The latest UI changes are on GitHub.
- Vercel project `dv3-yuria-web` is connected to GitHub repository `psycho909/dv3-yuria-web`. The Vercel Project Settings > Git page shows the connected-repository deployment description and a deploy hook for branch `main`. This confirms the Git integration is configured; verify the deployment created by this handoff push and production alias before claiming the UI is live.
- V2.1 remains isolated research code. Production recommendation stays V1 and deterministic probability/score math remains code-owned; Jev/LLM must not replace it. Read `B_yuria-web-spec/RECOMMENDATION-ALGORITHM_V2.1.md` before continuing.
- Current experimental files are `B_yuria-web-spec/src/recommendation-v2.ts`, `src/recommendation-v21.ts`, `tests/recommendation-v2.test.ts`, `tests/experiments/v1-baseline.test.ts`, and `tests/experiments/v2-paired.test.ts`. V2.1 module currently typechecks but has no dedicated V2.1 correctness tests yet. Do not use it as a recommendation result until those tests pass.
- Verification on this checkpoint: `npm run typecheck` passed. `npm test` passed 6 files, 29 tests; 5 were skipped, including opt-in policy experiments. Default-sandbox execution first failed because esbuild could not spawn (`EPERM`); rerun with approved process permissions succeeded. No V2.1 benchmark has run yet.
- Preserve the V2.1 specification limits: expected final score only; bounded depth 2; default scenarioCount 256 and pilotSamples 32; exact fifth-turn solve; use shared seeds/paired 30-game corpus only once ready; keep production V1. Do not run the S512/S1024/S2048 matrix unless the spec's S256 acceptance gate justifies it.
- Immediate next steps: add V2.1 fixtures for delayed blue, delayed purple, and World synergy; test special/round-five exact handling and deterministic ranking; then run the paired 30-game V1/V2/V2.1 benchmark and record mean score, paired differences, runtime, model (none; deterministic code), and limitations in Markdown. Finally run typecheck, tests, and build.
- Secrets are intentionally excluded from this handoff.

### Suggested Skills

- `matt-skills-curated:implement` for the bounded experimental implementation and tests.
- `matt-skills-curated:ml-best-practices` for benchmark design and paired statistical reporting.
- `vercel:deployments-cicd` for verifying the deployment created by the Git push.

### Exact Resume Action

Read `B_yuria-web-spec/RECOMMENDATION-ALGORITHM_V2.1.md`; add isolated tests around `src/recommendation-v21.ts` for delayed blue, delayed purple, World synergy, and exact Round 5; keep the production worker on V1.

## 2026-09-23 Vercel Git auto-deployment setup

- Vercel Settings > Git confirmed repository `psycho909/dv3-yuria-web` is connected to the project and production branch `main`.
- Initial push `9776ff5` created a production deployment, but the deployment URL returned 404 because Root Directory was blank for this monorepo. Fixed Vercel Settings > Build and Deployment by setting Root Directory to `B_yuria-web-spec`.
- Redeployed the same commit with the latest project settings as deployment `j5y7vTzAcPk3b8Bf9umADkA4HtwA`; Vercel reported Ready / Production. The production alias `https://dv3-yuria-web.vercel.app/` now loads the calculator and exposes the new 「版面寬度」滿版/窄版 controls.
- Git auto-deploy is enabled for `main`; future pushes now build from `B_yuria-web-spec`. No Vercel token or OAuth secret is stored in the repository.

## 2026-09-24 V2.1 correctness-test checkpoint

- Added `B_yuria-web-spec/tests/recommendation-v21.test.ts` with isolated delayed-blue, delayed-purple, World-synergy, and exact Round 5 fixtures. The targeted suite passed 4/4; production recommendation wiring remains on V1.
- Added the opt-in `B_yuria-web-spec/tests/experiments/v21-paired.test.ts` harness for the specified S256 / pilot 32 / depth 2 comparison across shared seeded worlds. It is skipped by default and does not change production behavior.
- The first resource probe for the specified configuration was not accepted as benchmark evidence: `RUN_V21_POLICY_EXPERIMENT=1 RUN_V21_GAMES=2` ended with Node `JavaScript heap out of memory` at approximately 3.9 GB heap after about 198 seconds. No V1/V2/V2.1 mean or runtime result was recorded.
- Next action: reduce the V2.1 retained-state/search expansion at the planner seam, add a regression for bounded memory, then rerun the exact 30-game benchmark. Do not lower S256 / pilot 32 or use the V2.1 planner in production to bypass this finding.
