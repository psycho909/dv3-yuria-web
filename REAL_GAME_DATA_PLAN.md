# 真實牌局資料計畫與實作狀態（2026-09-23）

## 目的與資料界線

五回合真實遊玩可用來核對推薦與遊戲結果。只有玩家完成五回合並按「紀錄本局」才進入多局資料庫；實際分數可留白，絕不由模型分數代填。A/B 的機率、計分及排序仍由 deterministic 程式計算；Jev 只可處理語意，不得代替數學。舊五筆僅有總分的實測與 30 場模擬，仍在獨立統計區，不混入本資料庫。

## 已實作的資料流程

1. `localStorage` 保存操作中的本局草稿；按「紀錄本局」後，`IndexedDB` 的 `yuria-real-games/games` 才保存完整一局。重新整理可接續；重設不刪除已紀錄局。
2. 每回合記卡 ID、顏色、成敗、高塔實際倍率與星星移除目標。從三張候選按下的回合，另外凍結候選、目標、當時推薦、三張預測與分位數；手動加入歷史的候選記為 `null`，不補猜。`catalogProbability` 是牌庫資料，`observedProbability` 在玩家未另行提供畫面機率時為 `null`。
3. 終局畫面分開顯示模型估算和可選填的遊戲實得分數。已紀錄局更正時增加修訂號並保留前版回合與分數；再次按「紀錄本局」才保存更正。
4. JSON 匯出／匯入採版本驗證與牌局 ID 去重；同 ID 的不同版本不默默覆寫。本機只讀統計區分已紀錄局、已填實得分數局、五回合候選齊全局，並顯示卡片點選後成功次數、候選顏色次數及有分數局的終局預測殘差。未選卡不當作失敗。樣本不足不顯示個人達標率。
5. 使用者選擇「獨立尤里亞 Supabase 專案」，同意組織 `kvmbbhbefiwljyzubssu` 與顯示費用 US$0／月；專案 `dv3-yuria`（東京 `ap-northeast-1`，ref `kihgibfacsvbmvvmohuc`）已建立。`public.real_games` 已啟用 RLS、擁有者政策及安全建議檢查。前端只含 publishable key，沒有 secret/service-role key。登入與雲端同步程式已接入，OAuth provider 的 Client ID／Secret 仍待在 Supabase/身分提供者控制台設定和實際登入驗證。

## 分析與校準門檻

- 玩家回報與畫面核對是不同證據等級；填分不代表有截圖。手動遊玩與依推薦選牌都有選擇偏差，不能用這些資料直接估算未選卡的結果。
- 顯示卡片點選後成功率前至少需 30 次該卡點選，並附 Wilson 95% 區間。終局殘差與個人分數至少各需 30 筆有實得分數及相應預測的完整紀錄；仍需按引擎版本與證據等級再分析。
- 更改紅色模型、出牌分布、特殊效果或推薦預設前，須有未參與調整的留出牌局、固定 seed 回歸和同資料集離線比較。真實資料不會自動改寫推薦公式。

## 驗收與待辦

- 本機 `npm run typecheck`、`npm test`、`npm run build` 通過；新增瀏覽器實測確認五回合結束前不入庫、可留白分數、事後更正、JSON 往返、重設仍保留紀錄及 0 page error。既有 18 項瀏覽器案例通過。
- 待 OAuth 設定後，以兩個帳號驗證登入與 RLS 隔離，再於正式站驗證跨裝置下載。未完成前不宣稱登入或雲端同步已可使用。
- 目前只保存五回合牌面和終局分數；遊戲畫面的分數拆解／祝福及真正顯示機率沒有輸入欄位，不能在分析時補猜。若未來新增欄位須升級 schema version。
- 已知原有 DESIGN 驗收限制：200% 瀏覽器縮放由使用者免驗；螢幕閱讀器未實測，瀏覽器 Worker P95 仍高於 800ms 目標。本資料功能不改寫這些結論。
