# 尤里亞占卜計算器 — UI / UX 設計規格

更新日期：2026-09-22。範圍：`B_yuria-web-spec/` 的完整單頁介面。

狀態：核心功能與本機／Vercel production 瀏覽器案例驗收通過；桌機瀏覽器計算效能未達 P95 目標，螢幕閱讀器仍待實測。200% 瀏覽器縮放由使用者決定免驗收，沒有宣稱實測通過。

## 1. 設計結論

採用「暖色卡牌桌面」：奶油色頁面、白色操作區、淡藍／淡紫／淡紅牌面、琥珀色主要操作。使用者看見的是正在進行的牌局，而不是統計儀表板。

核心流程只有一條：**填入遊戲候選 → 比較推薦 → 在遊戲中選牌 → 回填實際結果 → 下一回合**。

桌機同時呈現左側已確定卡片與右側三張候選；手機先呈現本回合操作，歷史以摘要加展開方式保留。數學模型、卡片數值與機率沿用現有 deterministic domain，不從參考圖片抄入。

### 成功指標

1. 初次使用者能在 5 秒內找到「選擇卡片」入口；此為待實測目標。
2. 熟悉介面後，每回合在 10–20 秒內完成三張牌與顏色輸入；計算時間另計。
3. 取消任何選牌／結果視窗都不改變正式盤面。
4. 一眼分辨「目前分數」「啟用率」「最終達標率」，不混為同一數字。
5. 375px 手機與 1440px 桌機都能完成五回合，不依賴橫向捲動或 hover。

## 2. 參考圖與取捨

| 參考 | 採用 | 改善／不採用 |
| --- | --- | --- |
| 第一張：空白牌局 | 暖奶油底、左右牌區、明確空槽、白色候選容器 | 放大字級和按鈕；讓下一步提示靠近候選，不靠大量留白猜操作 |
| 第二張：牌庫視窗 | 搜尋置頂、完整牌庫、牌名／效果／啟用率同時可見 | 改為亮色牌面；手機減少欄數；保留顏色、確認和取消；加焦點管理 |
| 第三張：進行中牌局 | 已選牌可修正、候選突出推薦、計分拆解、獎勵與牌庫置下方 | 不照搬微小控制、整頁密集數字、AI 標籤、獎勵道具 ID 或未確認獎勵數值 |

參考來源為使用者本次提供的三張截圖。第三張辨識名稱為 `FireShot Capture 002 - 尤里亞的占卜計算器 · dv3ex - [dv3ex.dv3ex3.workers.dev].png`。未將參考站的導覽列、圖鑑或其他工具擴充為本專案功能。

### ui-ux-pro-max 的使用方式

已讀取 `C:/Users/pc/.agents/skills/ui-ux-pro-max/SKILL.md`，執行 design-system、UX 與 HTML stack 查詢。採用高對比、44px 點擊區、清楚焦點、漸進揭露、表單標籤與狀態回饋。

自動 design-system 查詢曾回傳深色、3D、企業入口或不適合繁體中文的字體搭配；這些與使用者明確要求及工具用途衝突，因此不採用。HTML stack 查詢僅取用 HTML 可及性原則，不引入 Tailwind、React 或新 UI 套件。

## 3. 資訊架構與頁面順序

| 區域 | 常態顯示 | 展開後顯示 |
| --- | --- | --- |
| 頁首 | 標題、一句用途、本局進度、目前分數摘要 | 使用說明 |
| 推薦目標列 | 拚達標／平均最高／保守選牌、目標分數 | 計算設定與假設 |
| 牌局工作區 | 已確定卡片、三張候選、結果記錄入口 | 卡片詳情、歷史修正 |
| 推薦摘要 | 推薦哪張、依據何種目標、一句原因 | P10／P50／P90、分數範圍、計算方式 |
| 下方參考區 | 目前計分公式摘要、獎勵門檻摘要 | 完整門檻、22 張牌庫、規則、實測與模型統計 |

統計、設定與資料可信度不佔用獨立常駐側欄。第一屏優先給操作；牌庫不是首頁主角。

## 4. 桌機版面

內容最大寬度 1280px，置中，頁面水平留白至少 24px。桌機双欄僅在 1200px 以上啟用，避免 1024px 把五張歷史牌擠成細條。

```text
尤里亞的占卜計算器                       第 3 / 5 回合
填入牌面，查看下一張的推薦。             目前估算 371 分

[拚達標] [平均最高] [保守選牌]  目標 [1500 分]  [計算設定]

① 本局已確定卡片 · 2 / 5          ② 本回合候選 · 已填 3 / 3
┌──────────────────────────┐    ┌────────┬────────┬────────┐
│ [已選牌] [已選牌] [＋加入] │    │ 候選 1 │ 候選 2 │ 候選 3 │
│  色／結果可修改            │    │ 牌面   │ 推薦   │ 牌面   │
│ [撤回上一回合] [重設本局]  │    │ 顏色   │ 顏色   │ 顏色   │
└──────────────────────────┘    │ 達標率 │ 達標率 │ 達標率 │
                                │ 平均分 │ 平均分 │ 平均分 │
                                └────────┴────────┴────────┘
                                推薦理由；待選牌：[記錄這張的結果]

目前計分：分數 × 倍率 × 紅色加成 = 估算分數  [查看拆解]
[獎勵門檻 ▾] [卡片資料 ▾] [規則與模型限制 ▾] [統計與實測 ▾]
```

左右欄約 45%／55%，間距 24–32px。歷史牌可換行，不以縮小文字強塞五欄。候選固定三欄，每張至少約 190px；達不到時整個工作區切換單欄。

不新增參考站的跨產品導覽。不放大型 hero、宣傳文案或動畫背景。

## 5. 手機與平板

| 寬度 | 版面 | 卡片呈現 |
| --- | --- | --- |
| 320–599px | 單欄，左右 16px 留白；320px 可降至 12px | 候選為三列緊湊卡，每列左牌面、右顏色與主要數字；歷史以摘要收合 |
| 600–1199px | 工作區上下排列 | 候選可三欄；歷史牌 3–5 欄依可用寬度自動換行 |
| ≥1200px | 左歷史、右候選 | 候選三欄，歷史保持可讀寬度 |

手機順序：標題／目標 → 本局摘要 → 三張候選 → 推薦理由 → 歷史詳細 → 參考資料。DOM 順序與鍵盤順序必須合理；不要只用 CSS 視覺換序造成焦點跳躍。

```text
尤里亞占卜               第 3 / 5 回合
[拚達標] [平均最高] [保守選牌]
目標 [1500 分]
本局 2 張 · 目前估算 371 分 [查看／修改]

本回合三張候選                 3 / 3
[牌面 1]  藍／紫／紅  達標率、平均分
[牌面 2]  藍／紫／紅  達標率、平均分  推薦
[牌面 3]  藍／紫／紅  達標率、平均分
推薦摘要 [查看計算詳情]

底部操作列：已選牌名       [記錄結果]
```

底部操作列只在候選完整且已選定待記錄牌時出現；頁面預留其實際高度及 `env(safe-area-inset-bottom)`。未選定時不顯示永久無效按鈕。開啟視窗時隱藏此列，避免雙重確認。

## 6. 視覺規範

### 色彩 tokens

| Token | 色碼 | 用途 |
| --- | --- | --- |
| `--page` | `#F4EFE3` | 全頁奶油暖底 |
| `--surface` | `#FFFFFF` | 候選容器、輸入、視窗 |
| `--surface-subtle` | `#F9F5EA` | 說明與次級區塊 |
| `--border` | `#DED5C5` | 純裝飾分隔線，不作唯一控制邊界 |
| `--control-border` | `#8A7C68` | 輸入框、未選取按鈕邊界 |
| `--text` | `#292E2B` | 標題、主要文字 |
| `--text-muted` | `#62665F` | 說明、次級標籤 |
| `--primary` | `#8B580E` | 主要按鈕、推薦邊框與分數重點 |
| `--on-primary` | `#FFFFFF` | 主要按鈕文字 |
| `--primary-soft` | `#EBE0CA` | 推薦摘要、公式底色 |
| `--blue-bg` / `--blue-text` | `#DFEDF8` / `#245781` | 藍牌 |
| `--purple-bg` / `--purple-text` | `#EEE4F7` / `#67418C` | 紫牌 |
| `--red-bg` / `--red-text` | `#F8E1DC` / `#963E36` | 紅牌 |
| `--success-bg` / `--success-text` | `#E7F1E9` / `#276149` | 成功結果 |
| `--failure-bg` / `--failure-text` | `#F3E5E0` / `#923F32` | 失敗或錯誤，配文字標籤 |
| `--focus` | `#245781` | 3px focus ring，外距 3px |

設計階段按 sRGB 相對亮度公式計算：主要文字／頁底 12.04:1，次級文字／頁底 5.11:1，白字／主按鈕 6.00:1；藍、紫、紅牌文字／牌底分別 6.39:1、6.30:1、5.53:1。這是指定 token 配對檢查，不代替實作後對所有 hover、disabled、疊色與焦點狀態的驗收。

整頁維持亮色，不跟隨系統自動切成暗色。失敗牌以灰暖底、文字「失敗」及虛線表達，不使用黑色遮罩壓低可讀性。色彩永遠附文字標籤。

### 字體、間距、外形

正文使用 `"Segoe UI", "Microsoft JhengHei", system-ui, sans-serif`；不新增外部字體請求。英文眉標可用 Georgia 斜體作小幅占卜風格點綴，中文與數字保持清晰。

| 項目 | 規格 |
| --- | --- |
| 主標題 | 桌機 32px、手機 24px，700，行高 1.3 |
| 區段標題／牌名 | 18–20px，600–700 |
| 正文／控制／輸入 | 16px，行高 1.5–1.7 |
| 補充資訊 | 14px；非操作性分類角標最低 12px |
| 主指標 | 28–32px，tabular-nums；長文警示不能塞入大數字位置 |
| 間距 | 4／8／12／16／24／32／48px |
| 圓角 | 牌面 10px、容器 14px、視窗 18px、按鈕 8px |
| 陰影 | 候選容器極淡、視窗一層；不用發光、玻璃模糊或厚重漸層 |
| 動態 | 背景／邊框 150–200ms；不以縮放使卡片或文字跳動 |

## 7. 卡片與候選比較

### 穩定的位置

候選槽 1／2／3 對應玩家遊戲画面，計算後維持原位置。使用排名標籤、推薦邊框和推薦摘要表達排序；禁止每次換色就移動卡片，避免使用者按錯。

牌面呈現分類、牌名、效果、啟用率；外層呈現顏色選擇、推薦結果和操作。牌面按鈕只負責編輯，不兼作最終提交。點「待選這張」才選定要記錄的候選；此時仍不改變歷史。

| 層級 | 預設顯示 | 說明 |
| --- | --- | --- |
| 身分 | 牌名、藍／紫／紅文字、牌面效果 | 三張候選身分不可重複 |
| 卡片機率 | 啟用率 | 表示這張牌成功啟用的機率 |
| 主要指標 | 依目標變化 | 達標率／平均最終分數／P10 |
| 次要指標 | 平均最終分數或達標率 | 不能讓不同模式看起來只改 badge |
| 詳細資料 | 收合的分位數、範圍、方法 | 說明分位數和抽樣限制 |

三張候選不必各一種顏色，允許同色。顏色按鈕至少 44px 高、相鄰間距至少 8px；選取態加勾號與 `aria-pressed`，不只換底色。

### 推薦目標

| UI 名稱 | 主要指標 | 既有資料對應 |
| --- | --- | --- |
| 拚達標（預設） | 最終 ≥ 目標分數的機率 | `objective.kind = threshold` |
| 平均最高 | 平均最終分數 | `objective.kind = expected` |
| 保守選牌 | P10，附「約 10% 結果低於此分數」 | `objective.kind = stability` |

預設目標保留 1500 分；1400／2000／2700 是可選獎勵門檻，不將 1500 標成遊戲正式獎勵門檻。自訂目標欄使用可見標籤、非負有限整數驗證；錯誤留在欄下，不暗自改回 1500。

「本模式推薦」與「平均分數最高」可以並列；切換模式後不能仍將任意第一張標成「達標率最高」。同分時使用 domain 排序和穩定次序，不讓 UI 隨機選推薦。

## 8. 搜尋選牌視窗

使用原生 `<dialog>`。桌機最大寬 880px、最大高 `calc(100dvh - 32px)`；手機為接近滿版的面板。視窗只保留一個內容捲動區，頂部搜尋與底部操作固定可見。

| 區域 | 行為 |
| --- | --- |
| 標題 | 指出「候選 2」或「加入已確定卡片」，不讓使用者猜目的 |
| 搜尋 | 可見標籤、搜尋牌名、清除按鈕；保留繁體中文輸入法組字，不逐字重建 input |
| 分類 | 全部／分數／倍率／特殊；保留各分類數量 |
| 牌庫 | 桌機 6 欄、平板 4 欄、手機 3 欄；不足以保留 14px 資訊時降 2 欄 |
| 頁尾 | 已選牌名＋顏色；取消、套用；未選牌時停用套用並提示原因 |

搜尋無結果顯示「找不到卡片，請換個名稱或清除搜尋」。已在本局或其他候選槽使用的牌顯示「本局已選／候選 1 已使用」，不可選取；允許保留正在編輯槽本身的原牌。

點選牌面僅更新草稿，點選顏色不立即提交。取消、Escape、關閉按鈕捨棄草稿，恢復開窗前的正式資料。點遮罩不提交，也不意外清除草稿。

開啟時焦點進入搜尋欄；Tab／Shift+Tab 留在視窗；關閉後回到原觸發按鈕。改選牌面與顏色須保留搜尋字串、焦點和捲動位置。搜尋結果數用 `aria-live="polite"` 提示，不逐字朗讀整個牌庫。

## 9. 已確定卡片與結果記錄

空局顯示一個「＋加入已確定卡片」，附註「若已在遊戲中選過牌，可從這裡接續」。不要預填愚者，也不要強迫從第 1 回合輸入候選才能補歷史。

已確定卡片顯示回合、牌面、顏色、成功／失敗、被移除狀態。卡片可在原位置修改顏色與啟用結果；進階修正可更換牌名，但必須重新檢查重複和特殊效果依賴。

### 記錄流程

1. 從候選選定卡片，點「記錄這張的結果」，或由歷史區加入卡片。
2. 視窗顯示牌名與顏色，提供「成功啟用」「啟用失敗」「取消」三個語意分明的選項。
3. 遇到高塔／星星，完成必要的實際結果欄位；預覽本次變更。
4. 按「完成記錄」後一次提交，更新歷史及回合，清空三張候選與舊推薦。
5. 焦點移到下一回合第一個空槽；第五張完成則移到結算標題。

選擇成功或失敗先修改草稿，最後才提交；取消和 Escape 永遠不是失敗結果。提交中阻止重複按鈕造成重複記錄。

### 特殊牌必要輸入

| 情況 | UI | 資料／限制 |
| --- | --- | --- |
| 高塔啟用 | 選擇遊戲實際顯示的高倍率或低倍率 | 由 `CARDS.tower` 顯示數值，填入 `towerProc`；不將未填誤當低倍率 |
| 星星啟用且有可移除牌 | 選擇实际被移除的牌 | 更新該牌 `removed`；不可選星星自身或已移除牌；可選集合依既有規則 |
| 星星沒有可移除牌 | 顯示無需選擇 | 不強制填不存在的目標 |
| 紅色效果尚未確定 | 以模型分布呈現 | 不將固定 RNG 的單次分數當實測或平均值 |
| 修改先前特殊牌結果 | 提示需要重新確認相關移除／高塔結果 | 必須同步修正關聯狀態，不保留失效效果 |

撤回上一回合應恢復該回合前的整份盤面（含被星星移除的牌），不能只刪除陣列末項。可用最多五份盤面快照，不需要建立通用 undo framework。已移除牌仍佔用已完成回合，亦不回到可選牌庫。

重設本局會清除牌局與候選，需確認；保留使用者的推薦目標和計算精度。一般改色、取消視窗不跳確認。刷新保留牌局屬既有產品規劃，若此輪未做，須在離開／重整前提示尚未保存；不可假裝已保存。

## 10. 計算與狀態回饋

| 狀態 | 顯示 | 可做什麼 |
| --- | --- | --- |
| 初始／候選不完整 | 空槽、已填 0–2／3；「再填一張即可比較」 | 選牌、改色、補歷史；不顯示假結果 |
| 候選完整 | 自動計算，原槽位置保留 | 修改輸入，不自動提交歷史 |
| 計算中 | 指標位置顯示「計算中」；已有結果明示待更新 | 輸入保持可用，暫停用記錄入口避免誤用過期結果 |
| 計算成功 | 三張結果、目前目標推薦、簡短依據 | 選定牌並記錄結果 |
| 計算失敗 | 「暫時無法計算，輸入已保留」＋重試 | 不清除已選牌，不冒用舊結果 |
| 全部零命中 | 「本次抽樣沒有達標，暫按平均分數比較」 | 保留各卡原始指標；非「不可能達標」 |
| 精確模型機率為零 | 「依目前模型，達標機率為 0%」 | 不使用「抽樣未命中」用語 |
| 本局完成 | 五張歷史、最終模型分布／範圍、對應門檻 | 修正紀錄、撤回最後回合、開始新局；不再推薦第六張 |

計算使用現有 seeded domain；Web Worker 與 requestId 依既有 SPEC 的設計落實。輸入變更採約 150ms debounce；只接受最新輸入的結果，重設、撤回和結束一局都使舊請求失效。

效能驗收沿用既有規格：桌機 P95 <800ms，中階手機 P95 <1.5s，需註明裝置、瀏覽器、樣本數與起迄定義。這是待量測目標，不是目前保證。回饋在 100ms 內可見；超過 300ms 必須有計算狀態。

## 11. 數字與可信度

| 名稱 | 意義 | 呈現規則 |
| --- | --- | --- |
| 目前估算分數 | 已記錄牌局在目前模型下的結果 | 隨機效果未決時顯示範圍／模型估算，不冒充實測 |
| 啟用率 | 卡片本身成功啟用的機率 | 從卡片資料讀取，與最終達標率分開 |
| 平均最終分數 | 選此牌並完成剩餘回合的期望結果 | 顯示「最終」，避免誤認立即得分 |
| 達標率 | 最終分数達到使用者目標的模型機率 | 明示門檻；百分比常態一位小數，詳細資訊保留精度 |
| P10／P50／P90 | 分布分位數 | 放詳情並提供中文解釋 |
| 分數範圍 | 抽樣觀測範圍或精確模型支援範圍 | 依計算方法標示，不把抽樣最小／最大視為保證上下限 |

小於顯示精度的正機率顯示「<0.1%」，不能四捨五入成 0% 後誤用零命中說明。整數分數使用千分位，平均分數最多一位小數。

目前 `calculateScore` 接受 RNG，固定傳入 `() => .5` 只得到某個模型結果，不能稱作精確期望。若最終範圍介面需要新輸出，須先補齊 domain 契約與測試，再呈現數字；UI 不另寫一份公式。

第五回合推薦的 `exact` 是「依目前模型列舉」；前幾回合為 Monte Carlo。詳細資訊顯示計算方法、抽樣數與 seed；精確結果不顯示虛構的 10,000 次抽樣。

常駐小字：「模型估算，祝福尚未納入」。太陽自計數、未來同類別等權、紅色模型等限制放進可展開說明；影響目前候選時在牌旁顯示短提示。使用「模型推薦」，不沿用截圖的「AI 推薦」宣傳標籤。

## 12. 下方參考資訊

目前計分摘要顯示 `floor(SUM × MULT × (1 + RED_BONUS))`；展開後分組呈現分數來源、加總倍率、有效顏色、失敗補償與移除排除。內容由現有資料生成，不以截圖數值硬編碼。

獎勵門檻摘要使用 `REWARD_THRESHOLDS`，標明目前落點與下一門檻；完整表預設收合。沒有驗證的道具名稱、掉落率、售價與抽獎倍率一律不創造。本輪不加入參考圖的獎勵倍率與售價試算。

22 張牌庫按分數／倍率／特殊分類，列牌名、啟用率、效果。手機每張為可讀資料列，不橫向縮小桌機表格。搜尋選牌視窗與此表共用 `CARDS`。

實測紀錄與模型自我模擬分開標題、樣本數與說明；不把 5 局實測和 30 局模型混成一個準確率。技術 seed、假設和公式不擠入主要操作按鈕。

## 13. 可及性與微互動

所有主要控制至少 44×44px；一般文字對比至少 4.5:1，必要控制邊界／焦點至少 3:1。低對比裝飾框可保留，但不可成為唯一辨識線索。

使用原生 button、label、input、select、dialog、details；避免可點擊 div 與巢狀 button。目標和顏色單選使用 radio 語意或一致的 aria-pressed buttons，不實作不完整的 tabs 鍵盤模式。

錯誤貼近欄位且有修正方式；提交時聚焦第一個無效欄位。歷史更新與計算完成以低干擾 live region 宣告，不把整個重繪頁面當公告。

鍵盤可完成搜尋、選牌、顏色、取消、結果記錄及撤回。畫面重繪不丟失焦點、草稿、捲動位置。`prefers-reduced-motion` 關閉非必要轉場；200% 縮放与長牌名不遮住操作，手機橫向仍可捲動至確認列。

## 14. 技術落地與現況差距

沿用 Vite + TypeScript + 原生 CSS。元件是責任劃分，不要求對應獨立框架或每個區块一個檔案。`main.ts` 負責頁面互動，`domain.ts` 保持可測試的數學邊界；需要 worker 時另設一個運算入口。

樣式最终集中維護語意 tokens，回收已淘汰的暗色規則；本次進行中的 `usability.css` 是迭代樣式，不應無止境新增覆蓋層。保留已安裝依賴，暫不新增框架、圖表套件、帳號或外部服務。

| 項目 | 本次規劃時的現況 | 目標差距 |
| --- | --- | --- |
| 暖色與基本卡片 | 已完成暖色 tokens、卡片狀態、左右工作區與 RWD 基線 | 待 §17 視覺／響應式驗收 |
| 選牌視窗 | 已完成原生 dialog、搜尋、分類、草稿取消、同牌排除與焦點回復 | 待 §17 UX-02／UX-03、鍵盤與手機驗收 |
| 歷史與結果 | 已完成成功／失敗／取消、特殊牌必要輸入、撤回與重設 | 待 §17 UX-06～UX-09 驗收 |
| 候選推薦 | 已固定遊戲槽位，並依達標率／預期／穩定切換主指標 | 待 §17 UX-04／UX-05 驗收 |
| 計算 | 已完成 150ms debounce、Web Worker、requestId、計算中／錯誤／重試回饋 | 待 §17 計算競態與 P95 量測 |
| 本局完成 | 已顯示平均、P10／P90、方法與模型限制 | 待 §17 UX-10 與數字語意驗收 |
| 保存與恢復 | 已以 localStorage 保存可恢復的本機牌局、目標與候選 | 待跨刷新與瀏覽器限制驗收 |

本文針對 UI 的新版版面取代 `B_yuria-web-spec/WIREFRAME.md` 的早期三欄構想及手機橫滑方案。`SPEC.md` 的數學、規則與安全限制仍有效；施工時同步對應的 RWD 與流程段落。舊文件中「先加入歷史再詢問結果」改為結果收集完成後一次提交，避免中途取消留下半筆資料。

## 15. 分階段施工與驗收

1. **視覺與架構**：已完成暖色 tokens、左右工作區、手機順序與收合參考資訊；文字對比與 375／768／1024／1440px 已驗收，200% 縮放待補。
2. **選牌與比較**：已完成搜尋／分類、草稿取消、同牌排除、原槽穩定與目標指標；待鍵盤與完整取消流程驗收。
3. **五回合資料流程**：已完成結果草稿、高塔／星星、修改、撤回、重設與結算；待特殊效果及第五回合案例驗收。
4. **運算回饋**：已完成 Worker、debounce、requestId、錯誤／重試、零命中與精確結果文案；待競態、效能與數字語意驗收。
5. **整合驗收**：核心 domain regression、typecheck、build、桌機／手機操作已完成；效能、200% 縮放、螢幕閱讀器與部署狀態另行記錄。

### 核心驗收案例

| 編號 | 操作 | 預期 |
| --- | --- | --- |
| UX-01 | 空局開頁 | 暖色、0／5、三個空候選、可補历史，無虛構推薦 |
| UX-02 | 搜尋月亮，改紫色後取消 | 候選與歷史不變，焦點回觸發點 |
| UX-03 | 選牌後重新開窗 | 草稿從正式牌與顏色初始化；其他槽／本局同牌禁用 |
| UX-04 | 三張候選完成後切目標／顏色 | 位置不變，主指標和推薦更新，無過期計算覆蓋 |
| UX-05 | 打開結果視窗後 Escape | 不增加回合，不記成失敗 |
| UX-06 | 記錄一般牌失敗 | 歷史顯示失敗；domain 依規則計補償；候選清空 |
| UX-07 | 記錄高塔或星星 | 必填特殊結果；取消無部分提交；分數與顏色同步 |
| UX-08 | 撤回星星回合 | 恢復被移除牌及前一盤面狀態 |
| UX-09 | 第五回合完成 | 顯示結算與模型限制，不再提供下一回合選牌 |
| UX-10 | 零命中／精確零機率 | 使用對應措辭，不宣称遊戲絕不可能達標 |
| UX-11 | 375px、橫向；200% 縮放免驗收 | 已測尺寸無頁面橫向溢出，視窗底部操作可達；200% 由使用者豁免，未實測 |
| UX-12 | 鍵盤與 reduced-motion | 焦點可見、dialog 可退出、主要流程不依賴動畫 |

施工驗證入口（在 `B_yuria-web-spec/`）：`npm test`、`npm run typecheck`、`npm run build`；瀏覽器案例獨立記錄。本文的設計完成不等於這些 UI 案例已通過。

## 16. 決策邊界與下一步

暖／亮色、單頁卡牌工具、沿用既有數學與操作改善，已有使用者方向。本文把細節定為施工預設，不新增不必要的批准關卡。

未核實的獎勵表、規則變更、新服務與正式部署不屬於本次 UI 規劃。本機未提交的介面試作保留；不得把早期 build／domain 測試通過當作新版完整 UI 驗收。

下一步：補做螢幕閱讀器檢查，並持續處理桌機瀏覽器計算效能；200% 縮放依使用者決定免驗收。已完成項目與證據以第 17 節為準。

## 17. 驗收紀錄

記錄日期：2026-09-23（Asia/Taipei）。核心功能與 production smoke 已驗收。勾選只代表有可重現證據，不代表已驗證螢幕閱讀器或所有真實裝置。

本次完成：暖色亮色版面、固定候選槽、搜尋與分類選牌、結果草稿提交、特殊牌必要輸入、撤回／重設、localStorage 恢復、Web Worker、150ms debounce、requestId 過期結果防護、計算錯誤／重試、目標指標切換、第五回合精確零機率文案與焦點管理。

### 驗收環境與證據

- 本機瀏覽器：Microsoft Edge 153.0.4234.48；網址：`http://127.0.0.1:4174`。
- 瀏覽器案例：18／18 通過，0 page error；測試尺寸 375、768、1024、1440px，四種尺寸均無橫向溢出，暖色背景為 `rgb(244, 239, 227)`。
- 對比／控制：抽查對比 5.29:1–12.18:1；dialog 可操作控制皆至少 44px；鍵盤搜尋、選牌、分類、取消、焦點回復與 reduced-motion 通過。
- 操作證據：[browser-results.json](B_yuria-web-spec/artifacts/acceptance/browser-results.json)；同目錄保留桌機、平板、手機與 picker／結算截圖。

### Production deployment

- 2026-09-23 更新：效能程式提交 `91dfbfa` 已推送 `origin/main`；Vercel deployment `dpl_fE6M8DgMzsTfcPLiTApGaJJB3pfY` 為 `READY`／`production`，正式 alias 指向該 deployment。HTTP 200，HTML 載入 `index-Bl5Uvdlj.js`／`index-29OOJ2wB.css`；新版本 Edge 鍵盤記錄失敗與撤回通過、0 page error。下列為前一次完整瀏覽器 18／18 驗收快照。
- Git：`dc144cb7c59cd7543290533bd786626fe568ecdc` 已推送至 `origin/main`，local／remote SHA 一致。
- Vercel：`dpl_HEdVT5Hz3LZnvPog6shM2MjLpxgB`，`READY`、`production`；[Inspect deployment](https://vercel.com/psycho909s-projects/dv3-yuria-web/HEdVT5Hz3LZnvPog6shM2MjLpxgB)。
- 公開網址：[dv3-yuria-web.vercel.app](https://dv3-yuria-web.vercel.app/)；HTTP 200、Age 0，公開 bundle 為 `index-cPFbVJmG.js`／`index-29OOJ2wB.css`。
- Production 瀏覽器案例：同一套 18／18 通過，0 page error；Git auto-deploy 尚未連接，後續 push 仍需 Vercel CLI 部署。
- Observability：`vercel logs --level error --since 1h` 回報 `No logs found`；本次為靜態頁，沒有 runtime function log 可掃描。

### 功能與資料流程

- [x] UX-01 空局：暖色亮底、0／5、三個空候選槽、可從歷史區加入已確定卡片。
- [x] UX-02 牌庫搜尋：搜尋月亮、切換紫色、取消後候選資料和焦點都恢復。
- [x] UX-03 重複排除：已確定牌與其他候選槽的同一張牌顯示已使用且不可選。
- [x] UX-04 固定槽位：候選結果重新計算後仍留在遊戲中的第 1／2／3 槽，只改變推薦順位。
- [x] UX-05 目標切換：達標率／預期分數／穩定三種目標更新對應主指標與推薦理由。
- [x] UX-06 記錄結果：成功、失敗、取消三種操作；取消與 Escape 均不得增加歷史或把取消算失敗。
- [x] UX-07 換回合：完成記錄後清空舊候選、回合加一、焦點移到下一個空槽。
- [x] UX-08 特殊牌：高塔要求實際倍率；星星要求可移除牌；取消不留下半筆特殊狀態。
- [x] UX-09 修改與撤回：修改顏色／成敗／移除狀態會重算；撤回能恢復上一回合前的完整盤面。
- [x] UX-10 第五回合：顯示結算、不出現第六回合候選或舊推薦。

### 視覺、響應式與可及性

- [x] 375px 手機：無非預期橫向溢出，候選卡、顏色按鈕和視窗頁尾可操作。
- [x] 768px 平板：工作區切換單欄，歷史和候選順序符合文件，不壓縮牌名。
- [x] 1024px 與 1440px 桌機：左右工作區比例、卡片寬度、計分摘要和收合參考區可讀。
- [免驗] 200% 瀏覽器縮放：使用者於 2026-09-23 明確決定跳過驗收；這是需求豁免，不是實測 PASS。
- [x] 鍵盤流程：搜尋、選牌、顏色、套用、取消與 dialog 焦點陷阱已通過；另以 `tests/keyboard-flow.cjs` 在 production Edge 153、768px 驗證全鍵盤選牌、記錄失敗與撤回，歷史由 0→1→0，無 page error。
- [ ] 螢幕閱讀器語意：本輪未啟動螢幕閱讀器，僅完成 DOM／ARIA 靜態與鍵盤檢查。
- [x] `prefers-reduced-motion`：瀏覽器案例確認主要流程仍可操作且非必要 transition 為 0s。
- [x] 對比檢查：抽查文字與控制配對均 ≥4.5:1，控制最小尺寸無低於 44px 的啟用按鈕。

### 計算、效能與交付檢查

- [x] 同 seed、同輸入、同規則輸出一致；新增固定 seed 的分布回歸後，`npm test` 共 15 tests 通過。
- [x] 計算中、失敗、重試、全零命中與精確零機率文案符合第 10／11 節，並以瀏覽器案例確認舊結果不覆蓋重設後輸入。
- [~] Web Worker、約 150ms debounce、requestId 已驗證。先前 Windows 10／Node v22.20.0／Intel i5-8400、5 組 3 候選且每候選 10,000 次樣本：turn 1 為 2186–2260ms，超過桌機 P95 <800ms。2026-09-23 優化 Monte Carlo 熱路徑、保留抽樣數與規則後，在 Intel Core Ultra 7 165H／Node v22.14.0 同機基準由 1254–1413ms 降至 615–683ms；獨立重跑 5 組為 605–689ms，turn 5 為 0.016–0.062ms。12 組推薦與 24 組候選結果和優化前逐位元組 JSON 相同。這些短樣本不能證明 P95；原 i5 與瀏覽器端仍待複測。
- [未達標] 2026-09-23 正式站 Edge 153／Intel Core Ultra 7 165H，三候選、每候選 10,000 次，30 次連續瀏覽器 Worker 計算：最小 566ms、中位 1039ms、P95 1314ms、最大 1328ms；不含 150ms debounce 與畫面更新。`tests/browser-worker-performance.cjs` 可重現。桌機 P95 <800ms 目標尚未達成；Node 計時不可代替瀏覽器結果。
- [x] `npm test`、`npm run typecheck`、`npm run build` 通過；`git diff --check` 通過。
- [x] 已產出桌機／手機／牌庫／結算截圖與 JSON 操作證據；Git SHA、Vercel deployment ID 與 production smoke 已回填本節。

### 未完成與後續

1. 針對已確認的瀏覽器 Worker P95 超標做 profiling 與優化，再在原 i5 和目前裝置複測；保留每候選 10,000 次抽樣與固定 seed 回歸，不以降低抽樣數掩蓋精度。
2. 補做實際螢幕閱讀器檢查；Playwright ARIA 快照可看到 main、具名選牌 dialog、搜尋與顏色群組，但不能取代螢幕閱讀器實測。200% 縮放為使用者豁免。
3. 本節已記錄本次 Git push 與 Vercel production 結果；Git auto-deploy 尚未連接，下一次 production 更新仍要使用已登入的 Vercel CLI。

### 2026-09-23 特殊卡更正 BUG 與回歸

- 已重現：星星成功移除愚者後，將星星歷史結果改成失敗，愚者仍顯示已移除且保留「移除：愚者」，導致分數錯誤。
- 修復：星星改為失敗會清除其移除目標並恢復該卡；改回成功或更換目標須明確選實際目標。高塔改為失敗會清掉分支，改回成功須選實際倍率。星星管理的移除勾選由其更正對話框操作，避免兩處狀態矛盾。
- 本機證據：`tests/history-special-edit.cjs` 通過；`npm test` 15/15、typecheck、build 及 `tests/browser-acceptance.cjs` 18/18 通過，0 page error。程式提交 `175294a` 已推送 GitHub；Vercel `dpl_57PMFmUAaLgZ9e8SKwuQ4MbT3Ybj` 為 `READY`／`production`，正式 alias HTTP 200 且載入 `index-Bm9x20Da.js`。正式站特殊卡更正與鍵盤記錄／撤回測試再次通過。

## 18. 真實牌局紀錄與登入（2026-09-23）

- 五回合操作仍使用原候選、選牌與結果流程。第五回合後才顯示「紀錄這一局」；模型估算是唯讀文字，遊戲實得分數為獨立可留白的輸入。玩家按「紀錄本局」才將完整局寫入 IndexedDB。重新整理、修改與重設不可造成未確認的歸檔。
- 已歸檔局更正時顯示修訂狀態，只有再次按紀錄才更新儲存版本。匯出 JSON／匯入 JSON 應保留來源、版本及未知欄位；重複 ID 不重複計數，不默默覆寫不同版本。歷史手動加入的候選維持未知，不填入臆測資料。
- 側欄「真實牌局紀錄」顯示已記錄局數、有實得分數局數、候選齊全局數；樣本不足不顯示個人達標率。`catalogProbability` 與 `observedProbability` 區分牌庫預設和畫面觀察。
- GitHub 登入與雲端下載放在獨立收合區；未登入仍可本機操作。登入後的新紀錄僅在玩家按「紀錄本局」後同步。Supabase OAuth provider 尚待控制台設定；正式站登入與雙帳號 RLS 隔離未實測，不列為已驗收。
- 本機新增 `tests/real-game-flow.cjs`：五回合前 0 筆歸檔、刷新仍 0、可留白紀錄、後補 840 分、修訂版本、JSON 重複匯入不加倍、重設保留歸檔及 0 page error；既有瀏覽器驗收 18/18 通過。詳細資料欄位與校準門檻見 `REAL_GAME_DATA_PLAN.md`。
- 已選卡版面修正：側欄各卡固定相同兩欄寬度與 184px 卡面高度，5 張依 2／2／1 換列；手機 599px 以下改單欄。`artifacts/real-game-flow/history-1.png`、`history-5.png`、`history-5-mobile.png` 顯示首張與後四張格式一致。375px 無橫向溢出；修正後 `tests/real-game-flow.cjs` 和既有 18 項瀏覽器案例再次通過。
