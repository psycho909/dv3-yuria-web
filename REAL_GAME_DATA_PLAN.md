# 真實牌局資料計畫（2026-09-23）

狀態：**草案，尚未進入實作**。前置核對確認 B 推薦引擎已接入，2026-09-23 production 瀏覽器案例 18／18 通過、0 page error；200% 縮放由使用者決定免驗收。實際螢幕閱讀器仍未驗收，瀏覽器 Worker P95 實測未達桌機目標，且本資料記錄功能尚未納入 `DESIGN.md` 的完整 UX／驗收規格。不能宣稱整體「無 BUG」或設計已全數完成。開始實作前，先處理既有驗收缺口並將本計畫的操作與驗收寫入 `DESIGN.md`。

## 目標與邊界

收集可核對的五回合真實遊玩紀錄，用來校準未知遊戲規則、候選出現分布及推薦結果。模擬局不得併入真實樣本；Jev 只處理語意目標，所有機率、計分與排序維持可重現的程式運算。此文件是實作規劃，尚未代表資料記錄功能已上線。

## 現況

- B 的 `src/main.ts` 只以 `yuria-web-session-v1` 在 `localStorage` 保存一份當前牌局、候選與最多五個撤回快照；重設會移除它，沒有跨局檔案。
- B 的 `REAL_SCORES` 是五筆硬編碼總分，不含足夠的候選與決策證據。A 的 `docs/GAMEPLAY_AUDIT.md` 已區分截圖、使用者回報與推論；舊紀錄不能反推缺漏回合。
- A/B 的模型仍有紅色隨機值、未來候選分布、星星移除、太陽自計數等明示假設。資料需能分辨這些假設，不能只記終局分數。

## 儲存策略

1. **本機先行**：保留現有 `localStorage` 作為正在操作的牌局草稿；另以 IndexedDB 儲存多局紀錄。完成一局、重設或關頁都不可默默覆寫已記錄局數；匯出與匯入 JSON 以便跨電腦接續。輸出檔案由使用者自行保管，不放入公開 Git 儲存庫。
2. **雲端延後**：有跨裝置自動同步或多人提交需求時，沿用同一版資料格式接 Supabase。資料表需有擁有者、明確存取政策與 RLS；前端不得放 service-role/secret key。同步失敗不影響本機紀錄。未經明示操作不自動上傳舊資料。

## `RealGameRecordV1` 必要欄位

| 層級 | 欄位 | 用途 |
| --- | --- | --- |
| 牌局 | `id`, `schemaVersion`, `source=real_manual`, `status=draft/awaiting_score/complete`, `createdAt`, `completedAt` | 排除模擬、去重與續填 |
| 模型 | `engineVersion`, `cardCatalogVersion`, `rulesSnapshot`, `seed`, `simulations` | 舊推薦可重現，不被新預設改寫 |
| 每回合 | `turn`, `offers[3]`：卡 ID、顏色、畫面顯示的獲得率；`objective`, `target` | 推估出牌分布與當時目標；空欄要標未知，不補猜 |
| 決策 | `recommendedCard`, 三張候選的當時預測快照（達標率、平均分、分位數）、`chosenCard`, `activated` | 比對實際選擇與預測；未點選牌不可當成失敗樣本 |
| 特殊結果 | 高塔實際倍率、星星移除目標、其他可觀測隨機結果；未知用 `null` | 區分可觀測規則與推論 |
| 結算 | `actualFinalScore`, 可選的畫面分數／倍率／紅色加成、`evidenceLevel`（畫面核對／玩家回報） | 計分殘差與規則比對；預測分數不得覆寫實得分數 |

五回合結束時先標 `awaiting_score`，由玩家輸入遊戲畫面的實得分數後才標 `complete`。撤回與編輯只修改該局草稿，保留修訂時間；已完成局若更正，增加修訂號並保留前一版本供稽核。舊五局作為 `legacy_partial` 匯入時保留來源和未知欄位，不計入需要完整三候選的分析。

## 實作順序與驗收

1. **資料邊界**：新增獨立 `real-game-record.ts` 的型別、版本、驗證與正規化；不修改 `domain.ts` 的計分公式。測試拒收重複候選、選擇不在候選中、不合法特殊結果，以及宣稱 `complete` 卻缺少實際分數的紀錄；`awaiting_score` 可暫時沒有實際分數。
2. **本機保存**：在現有選牌、確認結果、修改、撤回、重設流程接入牌局草稿；選牌當下才凍結三候選預測快照。每次成功儲存顯示狀態；儲存失敗要可見且可匯出，不宣稱已保存。
3. **結算與攜帶**：第五回合加入「輸入實得分數」與完成確認；增加局數清單、JSON 匯出／匯入、版本驗證、重複 ID 避免覆寫。測試重新整理、重設、匯入匯出往返，以及完整五回合的瀏覽器流程。
4. **只讀分析**：先分開顯示樣本數、牌卡點選後成功率、候選顏色／類別頻率、最終分數預測誤差與未知規則案例；按 `engineVersion` 和證據等級分組，報信賴區間及缺漏數。不能以未點選牌驗證獲得率，也不能把反事實模擬當實得分數。
5. **受控優化**：只有在資料足夠且留出未參與調整的牌局驗證後，才比較命名規則設定／出牌模型版本；固定 seed 回歸、同一資料集的離線比較與真實牌局追蹤都通過，才考慮更改預設。Supabase 同步另開階段，不是本機記錄的前置條件。

## 驗收指標

- 一局五回合可在重新整理後接續，完成後能匯出包含三候選、實際選擇、成敗、特殊結果與實得分數的 JSON；開新局不會刪掉舊局。
- 匯入同一 JSON 兩次不會加倍計數；不完整、模擬與 `legacy_partial` 不進入「完整實測」分母。
- 所有統計標明樣本數與版本，資料不足時顯示「樣本不足」，不顯示虛假的個人達標率。新增記錄功能不改變同 seed 的現有推薦輸出。

## 開發與資料風險

- 手動輸入可與遊戲畫面不一致；保留玩家回報／畫面核對等級，不能自動升格成截圖證據。
- 玩家依推薦挑牌，資料有選擇偏差。點選後成功率只用實際點選的牌估計；未選牌效果與換策略後收益仍需模型或受控比較。
- IndexedDB 本機資料會因清除網站資料、無痕模式或裝置故障而遺失；匯出／匯入是第一階段的跨裝置交接方式。
